import { API_URL } from "../config";

export const LOGIN = `${API_URL}/api/login`;

export const REGISTER = `${API_URL}/api/register`;

export const GETTODOS = `${API_URL}/api/todolist`;
export const DelTodo = `${API_URL}/api/deleteTodo`;
export const CreateTodo = `${API_URL}/api/createTodo`;
export const MarkTodo = `${API_URL}/api/markTodo`;
